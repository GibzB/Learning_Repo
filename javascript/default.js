// default values in a function

function displayGreeting(name, salutation='Hello') {
    console.log(`${salutation}, ${name}`);
  }

    displayGreeting('Christopher', 'Hi');
// displays "Hi, Christopher!" when run